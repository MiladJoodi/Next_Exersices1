const handler = (res, req) => {


    if(req.method === 'GET'){
        //code=> Get all todos


    }else if(req.method === 'POST'){
        //code=> Create new todo
        


    }else{
        return false
    }
}

export default handler